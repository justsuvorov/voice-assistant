Вот краткое техническое описание для Readme.md, оформленное в строгом стиле для репозитория бэкенда.

Voice Assistant AI Core
Микросервис обработки медиа-данных на базе FastAPI. Выполняет роль вычислительного узла в распределенной системе (Bot <-> API <-> DB).

🏗 Технический стек
Runtime: Python 3.10+ (FastAPI)

STT Engine: OpenAI Whisper (инференс на CPU/GPU)

LLM Engine: Google Gemini 1.5 Flash

Database: SQLAlchemy 2.0 (PostgreSQL)

System Deps: FFmpeg (для декодирования аудио)

⚙️ Ключевые особенности
Model Warm-up: Загрузка весов Whisper в RAM при старте (Lifespan), что снижает Latency обработки до минимума.

Atomic Operations: Обновление статуса обработки и результата в БД происходит транзакционно.

Shared Storage: Работа с аудиофайлами через общие Docker Volumes (без пересылки бинарных данных по HTTP).

Regex Cleaning: Пост-процессинг ответов LLM для удаления технических префиксов и артефактов генерации.

📡 API Contract
POST /api/update — Точка входа для обработки задачи.

Request (JSON):

JSON
{
  "message_id": 1024
}
Workflow:

По message_id из PostgreSQL извлекается путь к файлу.

Whisper выполняет STT)

PromptEngine собирает контекст (Role + Transcription + Few-shot примеры).

Gemini генерирует контент.

PostProcessor нормализует текст.

Результат сохраняется в колонку result_post и возвращается клиенту.

🛠 Развертывание (Dev)
Установка системных зависимостей:

Bash
sudo apt update && sudo apt install ffmpeg
Настройка окружения:
Скопируйте .env.example в .env и укажите GEMINI_API_KEY и DATABASE_URL.

Запуск:

Bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000